# 1\. 系统定义

本系统是企业内部通用的消息投递平台，核心职责是接收业务系统提交的外部HTTP\(S\)通知请求，通过统一抽象接入、全生命周期状态管理，实现消息的可靠投递，聚焦“至少一次投递”语义，不承担外部系统的幂等、保序实现及业务逻辑校验，仅负责消息的接收、调度、持久化与投递执行。

# 2\. 系统边界

## 2\.1 负责解决

- 统一外部HTTP\(S\)通知抽象：屏蔽不同第三方供应商API差异，向上游业务系统提供统一接入方式；

- 可靠投递全生命周期管理：实现消息的接收、调度、持久化、投递执行及异常处理，确保消息不丢失、不漏发；

- 提供bizKey作为业务标识支撑，供外部系统实现幂等、保序逻辑（平台不负责具体实现）。

## 2\.2 明确不解决

- 消息内容合法性校验：业务参数的正确性、字段完整性、业务逻辑合理性，由上游业务系统负责；

- 外部系统业务落地：不保证外部系统对消息的处理结果、数据一致性，仅保证HTTP请求成功发出；

- 额外加工逻辑：不做接口配置化、参数拼装、加解密、签名等操作；

- 幂等与保序实现：由外部业务系统自行实现，平台仅提供bizKey支撑。

# 3\. 关键定义

## 3\.1 通知投递语义

采用**至少一次投递（At\-Least\-Once）**语义，核心约束：保证消息不丢失、必送达，容忍重复投递，幂等、保序由外部业务系统实现。

## 3\.2 外部系统失败/长期不可用处理策略

整体策略：有限次数重试 \+ 死信管理，具体如下：

- 瞬时失败（网络超时、非2xx响应）：执行有限次数重试（MVP阶段设为3次），采用固定间隔退避策略；

- 重试耗尽：消息进入死信管理，停止自动重试，避免流量风暴，需人工触发重投；

- 成功判定：仅当外部系统返回HTTP 2xx状态码时，视为投递成功。

# 4\. 整体架构

本系统采用模块化架构，以“职责单一、低耦合、可扩展”为核心原则，共划分4个核心模块，各模块边界清晰、交互简单，贴合落地需求，具体如下：

## 4\.1 核心模块划分及职责

### 4\.1\.1 接入模块

核心定位：请求入口，负责接收上游业务系统的通知请求，完成基础校验并转发合法请求。

- 核心职责：提供统一HTTP接口，接收业务系统提交的通知请求；校验请求必填字段（仅判断存在性，不校验合法性）；同步返回接收结果，不阻塞上游业务系统；将合法请求转发至投递调度模块。

- 输入：业务系统提交的通知请求（含bizKey、请求内容等）；

- 输出：请求接收结果（成功/失败）；合法请求转发至投递调度模块。

### 4\.1\.2 投递调度模块

核心定位：平台核心调度中心，负责任务全生命周期管理与各模块协同。

- 核心职责：将接入模块转发的合法请求封装为标准任务（生成唯一任务ID）；管理任务状态流转（状态机逻辑）；协调持久化模块完成数据存储；下发投递指令至投递执行模块；接收投递结果并更新任务状态。

- 输入：接入模块转发的合法请求；投递执行模块反馈的投递结果；

- 输出：标准任务信息；状态更新指令；投递指令；死信转发指令。

### 4\.1\.3 持久化模块

核心定位：负责任务与消息的可靠存储，确保数据可追溯，不涉及具体存储组件细节。

- 核心职责：接收投递调度模块下发的任务信息与状态更新指令，通过统一持久化机制完成数据存储；提供任务状态、异常信息的查询能力，支撑问题排查与审计需求。

- 输入：投递调度模块下发的任务信息、状态更新指令；

- 输出：持久化结果；任务状态/消息查询结果。

### 4\.1\.4 投递执行模块

核心定位：负责实际投递执行，对接外部系统。

- 核心职责：接收投递调度模块的投递指令，发起外部请求，接收外部系统响应，封装投递结果并反馈至投递调度模块。

- 输入：投递调度模块下发的投递指令、消息内容；

- 输出：投递结果（成功/失败、错误信息）。

## 4\.2 模块交互逻辑

各模块通过约定接口协同工作，形成完整业务闭环，核心流程如下：

1. 业务系统向接入模块提交通知请求；

2. 接入模块校验请求合法性，将合法请求转发至投递调度模块；

3. 投递调度模块封装任务、生成唯一ID，向持久化模块下发存储指令；

4. 持久化模块完成数据存储，反馈存储结果至投递调度模块；

5. 投递调度模块下发投递指令，投递执行模块发起外部请求并反馈结果；

6. 投递调度模块根据结果更新任务状态，同步至持久化模块；

7. 死信场景下，人工触发重投，完成闭环。

